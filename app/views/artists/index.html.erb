<% @artists.each do |artist| %>

<%= link_to artist.name, artist_path(artist) %> has <%= artist.song_count %> songs.<br>
  <% artist.songs.each do |song| %>

<ul><%= link_to song.title, song_path(song) %></ul><br>


  <% end %>

<% end %>
